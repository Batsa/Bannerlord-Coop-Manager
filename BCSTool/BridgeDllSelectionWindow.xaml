<Window x:Class="BCSTool.BridgeDllSelectionWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Bridge DLL Selection - Bannerlord Coop Manager"
        Width="700"
        Height="600"
        MinWidth="560"
        MinHeight="440"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False">
    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="MinHeight" Value="32"/>
            <Setter Property="Padding" Value="12,5"/>
        </Style>
    </Window.Resources>

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,12">
            <TextBlock Text="Bridge DLL Selection"
                       FontSize="22"
                       FontWeight="SemiBold"/>
            <TextBlock x:Name="TargetModText"
                       Margin="0,4,0,0"
                       FontWeight="SemiBold"
                       TextWrapping="Wrap"/>
        </StackPanel>

        <Border Grid.Row="1"
                Margin="0,0,0,12"
                Padding="10"
                BorderBrush="#9BB7D3"
                BorderThickness="1"
                Background="#F3F8FC"
                CornerRadius="4">
            <TextBlock TextWrapping="Wrap"
                       Text="Choose which declared DLLs the bridge should include. All are selected by default on a fresh install, and selecting none is allowed. For clients, remove or comment the same excluded declarations manually; BCS will not overwrite Workshop XML."/>
        </Border>

        <Grid Grid.Row="2">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBlock x:Name="SelectionSummaryText"
                       Grid.Row="0"
                       Grid.Column="0"
                       Margin="2,0,12,8"
                       VerticalAlignment="Center"
                       Foreground="Gray"/>
            <Button Grid.Row="0"
                    Grid.Column="1"
                    Margin="0,0,8,8"
                    MinWidth="96"
                    Content="Select All"
                    Click="SelectAll_Click"/>
            <Button Grid.Row="0"
                    Grid.Column="2"
                    Margin="0,0,0,8"
                    MinWidth="96"
                    Content="Clear All"
                    Click="ClearAll_Click"/>

            <ListBox x:Name="DllList"
                     Grid.Row="1"
                     Grid.Column="0"
                     Grid.ColumnSpan="3"
                     HorizontalContentAlignment="Stretch"
                     ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                <ListBox.ItemContainerStyle>
                    <Style TargetType="ListBoxItem">
                        <Setter Property="Padding" Value="7,6"/>
                        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                    </Style>
                </ListBox.ItemContainerStyle>
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <CheckBox IsChecked="{Binding IsSelected, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                  VerticalContentAlignment="Center">
                            <TextBlock Text="{Binding DllName}"
                                       FontFamily="Consolas"
                                       TextWrapping="Wrap"/>
                        </CheckBox>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </Grid>

        <Grid Grid.Row="3" Margin="0,14,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <Button Grid.Column="1"
                    Margin="0,0,8,0"
                    MinWidth="96"
                    Content="Cancel"
                    IsCancel="True"
                    Click="Cancel_Click"/>
            <Button Grid.Column="2"
                    MinWidth="96"
                    Content="Apply"
                    IsDefault="True"
                    Click="Apply_Click"/>
        </Grid>
    </Grid>
</Window>
